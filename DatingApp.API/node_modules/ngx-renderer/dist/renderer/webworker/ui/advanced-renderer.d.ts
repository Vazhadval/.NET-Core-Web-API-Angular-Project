import { ServiceMessageBrokerFactory } from '@angular/platform-webworker';
import { Serializer } from '@angular/platform-webworker/src/web_workers/shared/serializer';
import { AdvancedRenderer } from '../../advanced-renderer';
export declare class AdvancedRendererUi {
    private brokerFactory;
    private renderer;
    private serializer;
    private broker;
    constructor(brokerFactory: ServiceMessageBrokerFactory, renderer: AdvancedRenderer, serializer: Serializer);
    setup(): void;
    private _callTask(task, args);
    private _deserializeArgs(arg);
}
